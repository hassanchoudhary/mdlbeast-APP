import { View, Text } from 'react-native';

// TODO: MAR-31 — Book cabana / sunbed. Date → time → party size → add-ons → summary → confirm.
// Native flow against PeopleVine API.
export default function PoolBookScreen() {
  return (
    <View>
      <Text>[MAR-31] Pool booking placeholder</Text>
    </View>
  );
}
