import { View, Text } from 'react-native';

// TODO: MAR-6 / MAR-29 / MAR-30 — Pool LP. Three states: no bookings, one upcoming, multiple.
// Hero + Strapi info content + Book Now CTA + booking cards.
export default function PoolLandingScreen() {
  return (
    <View>
      <Text>[MAR-6 / 29 / 30] Pool landing placeholder</Text>
    </View>
  );
}
