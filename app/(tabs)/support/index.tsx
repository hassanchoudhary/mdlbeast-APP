import { View, Text } from 'react-native';

// TODO: MAR-51 — Contact Us. WhatsApp, phone, email channels + native message form.
export default function SupportLandingScreen() {
  return (
    <View>
      <Text>[MAR-51] Contact Us placeholder</Text>
    </View>
  );
}
