import { View, Text } from 'react-native';

// TODO: MAR-61 — Profile view + edit. Name, mobile, email, marketing prefs.
// Edits saved via PeopleVine API.
export default function SettingsProfileScreen() {
  return (
    <View>
      <Text>[MAR-61] Profile / settings placeholder</Text>
    </View>
  );
}
