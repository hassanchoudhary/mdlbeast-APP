import { View, Text } from 'react-native';

// TODO: MAR-39 [Webstore] — SevenRooms Webstore in WebView. Pre-fill member_id, email.
// Listen for return deep-link to confirm/cancel reservation.
export default function RestaurantBookScreen() {
  return (
    <View>
      <Text>[MAR-39] Restaurant booking (SevenRooms WebView) placeholder</Text>
    </View>
  );
}
