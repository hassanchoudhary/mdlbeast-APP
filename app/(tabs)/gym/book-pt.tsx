import { View, Text } from 'react-native';

// TODO: MAR-36 [Webstore] — WebView to MDLBeast webstore for PT booking.
// Pre-filter to PT services. Handle return deep-link to invalidate bookings cache.
export default function GymBookPtScreen() {
  return (
    <View>
      <Text>[MAR-36] Book PT (WebView) placeholder</Text>
    </View>
  );
}
