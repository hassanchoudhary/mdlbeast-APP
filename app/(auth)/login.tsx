import { View, Text } from 'react-native';

// TODO: MAR-5 — 4-digit passcode entry, 5-attempt lockout, FaceID shortcut (MAR-21)
export default function LoginScreen() {
  return (
    <View>
      <Text>[MAR-5] Login (passcode) placeholder</Text>
    </View>
  );
}
