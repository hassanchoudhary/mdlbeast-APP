import { View, Text } from 'react-native';

// TODO: MAR-22 — SMS OTP verify → set new 4-digit passcode → update SecureStore.
export default function ForgotPasscodeScreen() {
  return (
    <View>
      <Text>[MAR-22] Forgotten passcode placeholder</Text>
    </View>
  );
}
