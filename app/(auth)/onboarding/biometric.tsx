import { View, Text } from 'react-native';

// TODO: MAR-20 — optional FaceID/biometric enrolment via Expo Local Authentication. Skip allowed.
export default function OnboardingBiometricScreen() {
  return (
    <View>
      <Text>[MAR-20] Biometric enrolment placeholder</Text>
    </View>
  );
}
